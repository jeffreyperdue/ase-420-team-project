"""Constants module initialization."""

from .game_dimensions import (
    WIDTH, HEIGHT, CELL_SIZE,
    WINDOW_WIDTH, WINDOW_HEIGHT, SCREEN_SIZE,
    FPS, START_X, START_Y
)

from .game_states import (
    START_SCREEN,
    PLAYING,
    GAME_OVER
)

from .colors import (
    BLACK, WHITE, GRAY, RED,
    COLORS, NEXT_PAGE_PREVIEW_RECT
)